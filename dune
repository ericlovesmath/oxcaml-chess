; To see the [asm] and [cmm] dumps in [<module>.s] and [<module>.cmx.dump],
; build with the profile [dune build --profile dump --cache=disabled]
(env
 (dump
  (ocamlopt_flags
   (:standard -S -dasm-comments -dcmm -dlinear -dump-into-file))))
